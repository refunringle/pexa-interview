import { Routes } from '@angular/router';
import { PaginatedTableComponent } from './paginated-table/paginated-table.component';

export const routes: Routes = [
  { path: '', component: PaginatedTableComponent }
];
